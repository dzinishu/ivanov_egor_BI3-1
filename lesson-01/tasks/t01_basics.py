"""Тема 1. Основы Python: числа, строки, выражения, форматирование."""
from pip._internal.utils import retry


def task_01(a, b, c):
    """Среднее арифметическое трёх чисел.

    Верните среднее арифметическое чисел a, b и c.
    Результат всегда должен быть числом с плавающей точкой (float),
    даже если все аргументы целые.

    Примеры:
        task_01(1, 2, 3) == 2.0
        task_01(10, 20, 40) == 23.333333333333332
    """
    return (a + b + c) / 3


def task_02(total_seconds):
    """Разложить количество секунд на часы, минуты и секунды.

    Верните кортеж (часы, минуты, секунды).
    Часы и минуты — целые числа от 0, секунды — целое число от 0 до 59.

    Примеры:
        task_02(3661) == (1, 1, 1)
        task_02(59) == (0, 0, 59)
        task_02(0) == (0, 0, 0)
    """
    hours = total_seconds // 3600
    minutes = (total_seconds % 3600) // 60
    seconds = total_seconds % 60
    return (hours, minutes, seconds)


def task_03(fahrenheit):
    """Перевести температуру из градусов Фаренгейта в градусы Цельсия.

    Формула: C = (F - 32) * 5 / 9. Результат — float.

    Примеры:
        task_03(212) == 100.0
        task_03(32) == 0.0
        task_03(98.6) == pytest.approx(37.0)
    """
    return (fahrenheit - 32) * 5 / 9.0


def task_04(price, discount_percent):
    """Цена со скидкой.

    price — исходная цена (float), discount_percent — процент скидки (например, 25).
    Верните итоговую цену, округлённую до двух знаков после точки.

    Примеры:
        task_04(1000, 25) == 750.0
        task_04(199.99, 10) == 179.99
        task_04(500, 0) == 500.0
    """
    final_price = price * (1 - discount_percent * 0.1)
    return round(final_price, 2)


def task_05(x, y):
    """Строка-отчёт о сумме двух чисел.

    Верните строку вида "x + y = сумма", где x, y и сумма подставлены
    как есть (без округления). Используйте любой способ форматирования:
    f-строку, str.format или конкатенацию.

    Примеры:
        task_05(2, 3) == "2 + 3 = 5"
        task_05(-1, 1) == "-1 + 1 = 0"
        task_05(1.5, 2) == "1.5 + 2 = 3.5"
    """
    return str(x) + " + " + str(y) + " = " + str(x + y)


def task_06(name):
    """Нормализовать имя.

    Уберите лишние пробелы в начале и конце, а также повторяющиеся пробелы
    между словами; каждое слово приведите к виду «первая буква заглавная,
    остальные строчные». Верните результат.

    Примеры:
        task_06("  аННА   пЕТРОВА  ") == "Анна Петрова"
        task_06("иван") == "Иван"
        task_06("  ЖАН-КЛОД  ван дАММ ") == "Жан-клод Ван Дамм"
    """
    return " ".join(word.capitalize() for word in name.split())
